services:
  - type: web
    name: ml-service
    env: python
    region: ohio
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
    envVars:
      - key: DATABASE_URL
        value: postgresql://neondb_owner:npg_9yw1VRLCucxB@ep-round-mud-ax6x00uk.c-4.us-east-2.aws.neon.tech/neondb?sslmode=require
      - key: LLM_PROVIDER
        value: huggingface
      - key: HUGGINGFACE_API_KEY
        sync: false # User must provide this in the Render dashboard
